	<div class="col-lg-8">
		<h3>Users</h3>
		<table class="table table-striped">
				<thead>
					<th>image</th>
					<th>name</th>
					<th></th>
				</thead>
			<% users.each do |user| %>
				<tbody>
					<% if user.profile_image.present? %>
						<td><%=attachment_image_tag  user, :profile_image%></td>
					<%else%>
						<td><img class="noimage" src='assets/noimage.jpg'></td>
					<%end%>
					<td>
						<%=link_to user_path(user.id) do %>
						<%=user.name%>
						<%end%>
					</td>
					<td>
						<%=link_to user_path(user.id) do %>Show<%end%>
					</td>
				</tbody>
			<% end %>
		</table>
	</div>